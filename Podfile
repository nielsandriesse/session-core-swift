platform :ios, '12.0'

target 'SessionCore' do
  use_frameworks!
  pod '25519', '~> 2.0.2', :inhibit_warnings => true # Unfortunately this doesn't work with Carthage

  target 'SessionCoreTests' do
    pod '25519', '~> 2.0.2', :inhibit_warnings => true # Unfortunately this doesn't work with Carthage
  end
end
